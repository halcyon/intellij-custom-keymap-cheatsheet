(ns intellij-custom-keymap-cheatsheet.core-test
  (:require [clojure.test :refer :all]
            [intellij-custom-keymap-cheatsheet.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
